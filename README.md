# SendFileToSerialPort
some code I put together to transfer a small audio file to an external memory chip via its TX/RX lines. I left out the code that sends over the file size to the chip but it's because for my quick use case I just programmed it into the MCU code since I already had it. check out my other repo Serial_SaveDataToExtMem for the mcu code
